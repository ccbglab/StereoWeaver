[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "axialchem"
version = "0.1.0"
description = "Axial chirality descriptor and screening pipeline"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["axialchem"]
